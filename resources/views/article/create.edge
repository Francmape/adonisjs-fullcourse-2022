@layout('layouts/main')

@section('content')

  <div class="m-auto mt-5">
    <form action="{{route('articles.store')}}" method="POST" class="container col-lg-6" enctype="multipart/form-data">
        <h1>Create Article</h1>
        {{ csrfField() }}
        <div class="mb-3">
            <label for="title" class="form-label">Article Title</label>
            <input type="text" class="form-control" id="title" name="title" placeholder="Article Title">
        </div>
        @if(flashMessages.has('errors.title'))
        <p class="alert alert-danger"> {{ flashMessages.get('errors.title') }} </p>
        @endif

        <div class="mb-3">
            <label for="email" class="form-label">Email</label>
            <input type="text" class="form-control" id="email" name="email" placeholder="Email">
        </div>
        @if(flashMessages.has('errors.email'))
        <p class="alert alert-danger"> {{ flashMessages.get('errors.email') }} </p>
        @endif

        <div class="input-group mb-3">
            <label class="input-group-text" for="image">Upload</label>
            <input type="file" name="image"  class="form-control" id="image" />
        </div>
        @if(flashMessages.has('errors.image'))
        <p class="alert alert-danger"> {{ flashMessages.get('errors.image') }} </p>
        @endif

        <div class="mb-3">
            <label for="content" class="form-label">Write article here</label>
            <textarea placeholder="Article Content" class="form-control" name="content" id="content" rows="3"></textarea>
        </div>
        @if(flashMessages.has('errors.content'))
            <p class="alert alert-danger"> {{ flashMessages.get('errors.content') }} </p>
        @endif

        <div class="d-grid gap-2">
            <button type="submit" class="btn btn-primary mb-3 block flex">Create</button>
        </div>
    </form>
</div>

@endsection